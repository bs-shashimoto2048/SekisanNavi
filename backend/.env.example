# backend/.env.example
#
# 実パスワードを含む .env はGit管理対象外とすること (.gitignore 参照)。
# 開発時は本ファイルをコピーして backend/.env を作成し、値を設定する。
#
#   cp .env.example .env
#
# 本番運用時の認証方式は未確定 (docs/implementation-plan.md 参照)。
# PoCでは環境変数からの単純な平文比較 (定数時間比較) のみを行う。

SEKISAN_NAVI_ADMIN_PASSWORD=change-me-for-local-dev
